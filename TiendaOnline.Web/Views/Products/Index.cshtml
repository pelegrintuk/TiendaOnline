@model IEnumerable<TiendaOnline.Application.DTOs.ProductDto>

@{
    ViewData["Title"] = "Gestión de Productos";
}

<h1>Gestión de Productos</h1>

@if (User.IsInRole("Admin"))
{
    <p>
        <a asp-action="Create" class="btn btn-primary">Crear Nuevo Producto</a>
    </p>
}

<table class="table table-striped table-hover">
    <thead class="thead-dark">
        <tr>
            <th>Imagen</th>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Precio</th>
            <th>Categoría</th>
            <th>Stock</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @if (item.Images != null && item.Images.Any())
                    {
                        <img src="@item.Images.First().ImageUrl" alt="@item.Name" class="img-thumbnail" style="width: 100px; height: auto;" />
                    }
                    else
                    {
                        <img src="default-image.jpg" alt="Imagen no disponible" class="img-thumbnail" style="width: 100px; height: auto;" />
                    }
                </td>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>@item.Price.ToString("C")</td>
                <td>@item.Category</td>
                <td>@item.Stock</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info" style="margin-right: 5px;">Detalles</a>
                    @if (User.IsInRole("Admin"))
                    {
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning" style="margin-right: 5px;">Editar</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger" style="margin-right: 5px;">Eliminar</a>
                    }
                    @if (item.Stock > 0)
                    {
                        <form asp-action="AddToCart" method="post" class="d-inline" style="margin-right: 5px;">
                            <input type="hidden" name="productId" value="@item.Id" />
                            <input type="hidden" name="quantity" value="1" />
                            <button type="submit" class="btn btn-primary">Añadir al Carrito</button>
                        </form>
                    }
                    else
                    {
                        <button class="btn btn-secondary" disabled>Sin Stock</button>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
